The main code is in the file full_dev.ipynb
dedup_state.pkl contains the lsh_index and the file hashes after making the graph i.e. reading all the files
graph.cypher is a serialized version of our graph (it contains commands to remake the graph from scratch)
logs.txt contains the logs of the events happening during extraction and structuring 
write_up_layer10.pdf contains a brief write_up explaining the work done for this assignment and how to make it work.
